# タスクリスト (ペースト・クリア競合不具合の根本解決)

画像個別クリア後に自動で画像が再ロードされてクリアできなくなる競合不具合を、ペーストコンポーネントの「ワンタイムメッセージIDプロトコル」導入により根本解決するタスク。

- [x] 設計と計画
  - [x] バグの根本原因の特定（再起動時のStreamlitコンポーネント状態残留による自動再ロード現象）
  - [x] ワンタイムIDプロトコル（`{ id, data }`）の設計
  - [x] 実装計画（`implementation_plan.md`）の作成
- [x] UIコンポーネントの修正 (JS側)
  - [x] `src/paste_bridge/index.html` 内の `handleFile` で送信するデータを `{ id: 'paste_xxx', data: base64 }` のオブジェクト形式に変更
- [x] アプリケーションの修正 (Python側)
  - [x] `app.py` のペースト判定処理を `{ id, data }` 形式に対応させ、`last_paste_id` で重複判定を行うように修正
  - [x] 不要となった一時的な `last_pasted_base64 = None` リセット処理の削除（整合性の正常化）
- [x] 動作検証
  - [x] Streamlit上での再起動・検証
  - [x] 画像ペースト ➔ 個別クリア ➔ 正しく画像が消えることの確認
  - [x] 個別クリア ➔ 同じ画像を再ペースト ➔ 2回目も正しくプレビューが表示されることの確認
  - [x] `walkthrough.md` の作成
