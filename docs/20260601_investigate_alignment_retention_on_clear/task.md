# タスクリスト (個別クリア時の設定保持に関する調査)

個別画像クリア時のアライメント設定（読取りエリアや有効項目）の保持能力に関する調査・解説タスク。

- [x] 設計と計画
  - [x] Streamlit側（`app.py`）のセッション状態（`st.session_state`）の生存期間の確認
  - [x] Canvas HUD側（`fitting_hud/index.html`）の `streamlit:render` 時の初期値同期プロトコルの分析
  - [x] 現状のコードベースにおける設定保持の挙動確認と証明
  - [x] 解説を含めた「実装計画（調査報告）」の作成
