# タスクリスト (Task List) - コピペステーションの機能強化およびUI日本語化

本タスクリストは、改修作業の進捗を記録・管理するためのものです。

- `[x]` **1. app.py の UI 表示修正**
  - `[x]` メインタイトルを「サカつく2026 パラメータOCRリーダー v0.1」に変更する。
  - `[x]` サブタイトルを「選手のパラメータタブの中身を読み取るツール」に変更する。
- `[x]` **2. app.py のコピペステーション機能追加・レイアウト調整**
  - `[x]` 「解析結果テーブルを空にする」ボタンの右（「コピペステーションに展開する」との間）に「コピペステーションをクリアする」ボタンを配置する。
  - `[x]` 「コピペステーションをクリアする」ボタン押下時に、セッション状態の `tsv_horizontal` と `tsv_vertical` を空文字にクリアして `st.rerun()` するロジックを実装する。
- `[x]` **3. app.py の TSV 展開ロジックの修正**
  - `[x]` 横展開TSV：`show_headers` が `False` の場合、出力する列から `"元画像名"` を除外して、純粋な「パラメータ値のみ」を出力するロジックを実装する。
  - `[x]` 縦展開TSV：`show_headers` が `False` の場合、`--- 【画像: xxx】 ---` デリミタ行を出力しないように修正する。
- `[x]` **4. 品質検証とコードクレンジング**
  - `[x]` `python -m py_compile app.py` を実行し、構文エラーがないことを検証する。
- `[x]` **5. 動作検証およびドキュメント作成**
  - `[x]` 手動で動作を確認し、UIの日本語化、コピペステーションのクリア、ヘッダーOFF時の完全数値化（画像名やデリミタ行の非表示）が横展開・縦展開の双方で正しく動作することを確認する。
  - `[x]` `walkthrough.md` を作成して変更内容と動作検証結果を記録する。
