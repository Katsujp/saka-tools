# タスクリスト：Googleドライブ連携の撤回およびローカルJSONファイルによる矩形位置情報保存・復元機能の実装

本タスクリストは、Google Drive/OAuth2のクリーンアップ、およびローカルJSONファイルを利用したアライメント調整データ（矩形位置情報）の保存・復元機能の実装進捗を管理するものです。

## チェックリスト

- [x] `task.md` の作成および構成定義
- [x] `app.py` から Google Drive API・OAuth2 関連ロジックの完全削除
- [x] `app.py` から起動時の復元確認ダイアログの完全削除
- [x] `app.py` のプレビューHUD下に、ローカル保存用のダウンロードボタン (`st.download_button`) の実装
- [x] `app.py` のプレビューHUD下に、ローカル復元用のファイルアップローダー (`st.file_uploader`) の実装
- [x] 復元用アップローダー経由でJSON展開・セッション上書き・再描画同期するハンドラーの実装
- [x] アプリの構文検証（`python -m py_compile app.py`）
- [x] 動作検証（ローカル保存で任意の場所に保存できるか、ローカル復元で値がHUDとスライダーに完全同期されるか）
- [x] 修正内容 of 確認ドキュメント (`walkthrough.md`) の作成
