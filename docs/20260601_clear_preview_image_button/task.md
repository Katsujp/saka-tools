# タスクリスト (プレビュー画像の個別クリアボタン追加)

プレビュー対象画像のプルダウンメニュー直下に、選択中の画像をリストから個別に削除（クリア）できる「画像をクリアする」ボタンを追加するタスク。

- [x] 設計と計画
  - [x] `app.py` における「プレビュー対象画像」セレクトボックスの実装箇所の特定
  - [x] 画像クリア時のセッション状態（`st.session_state.target_images`）削除ロジックの設計
  - [x] 実装計画（`implementation_plan.md`）の作成
- [x] アプリケーションの実装
  - [x] `app.py` への「画像をクリアする」ボタン（`st.button`）の追加
  - [x] 選択中の画像を削除するセッション状態操作および画面再描画（`st.rerun()`）処理の実装
  - [x] Apple Pro漆黒ミニマリズムに合わせたボタンスタイリングの適用
- [x] 動作検証
  - [x] Streamlit上での起動テスト
  - [x] 画像複数枚ロード時の個別削除機能の動作確認
  - [x] 画像全削除時のCanvas HUD非表示および再ロード動作の確認
  - [x] `walkthrough.md` の作成
